spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:h2:mem:authdb
    username: auth
    password: secret
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      path: /auth-console
      enabled: true
  sql:
    init:
      mode: always
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  port: 4004

jwt:
  secret: eoiuifrwehgfuweufgweur382r832rhwqjddqjwehqw